<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".objetivo3.farmancias"></activity>
        <activity android:name=".objetivo3.brigadasDeSalud" />
        <activity android:name=".objetivo3.ajendaCita" />
        <activity android:name=".objetivo3.consultaNSS" />
        <activity android:name=".objetivo3.centroDeSalud" />
        <activity android:name=".objetivo3.MenuObjetivo3" />
        <activity android:name=".objetivo3.DetallesObjetivo3" />
        <activity android:name=".DetallesObjetivo1" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>